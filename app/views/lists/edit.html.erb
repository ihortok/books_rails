<%= render partial: 'shared/breadcrumbs', locals: { links: [{ title: t('lists.index.title'), url: lists_path }, { title: @list.title, url: list_path(@list) }], title: t('actions.edit_record.list') } %>

<%= render 'form', list: @list %>
