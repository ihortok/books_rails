<%= render partial: 'shared/breadcrumbs', locals: { links: [{ title: t('authors.index.title'), url: authors_path }, { title: @author.name, url: author_path(@author) }], title: t('actions.edit_record.author') } %>

<%= render 'form', author: @author %>
