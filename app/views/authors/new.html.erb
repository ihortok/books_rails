<%= render partial: 'shared/breadcrumbs', locals: { links: [{ title: t('authors.index.title'), url: authors_path }], title: t('actions.add_record.author') } %>

<%= render 'form', author: @author %>

